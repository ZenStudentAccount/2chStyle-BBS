services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./prisma:/app/prisma
      - ./users.htpasswd:/app/users.htpasswd
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:/app/prisma/dev.db
    restart: always
